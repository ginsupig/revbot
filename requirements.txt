# Core runtime dependencies for the live bot and its PnL/fees reports.
#   Install:  pip install -r requirements.txt
# For tests + the research backtests, use requirements-dev.txt instead.

numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
requests>=2.31
python-dotenv>=1.0            # provides the `dotenv` module (NOT the `dotenv` PyPI pkg)

# Market data — alpaca-py. Used by run_real_backtest.fetch_alpaca_bars, which
# main.py calls for bars.
alpaca-py>=0.20

# Trading / account / orders — legacy SDK used by execution.py, pnl_report.py,
# and fees_report.py. EOL/unmaintained; scheduled for migration to alpaca-py's
# TradingClient. Capped below 4.x to avoid a surprise breaking major.
alpaca-trade-api>=3.1,<4
